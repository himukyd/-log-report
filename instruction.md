# Task

An Apache-style access log is available at `/app/access.log`.

Generate a JSON report named `report.json`.

## Success Criteria

1. Create `/app/report.json`.
2. The report must contain:
   - total_requests
   - unique_ips
   - top_path